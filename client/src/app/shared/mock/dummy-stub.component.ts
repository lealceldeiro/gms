import { Component } from '@angular/core';

/**
 * A dummy (mock) component, useful for assigning it to routes being tested with RouterTestingModule#withRoutes
 */
@Component({ selector: 'gms-stub', template: ''})
export class DummyStubComponent {}
